import LangChanger from "./LangChanger"

const Layout = ({ children }) => {
  return (
    <>
        {/* Navbar */}
        {children}
        
        {/* Navbar */} 
    </>
  )
}

export default Layout